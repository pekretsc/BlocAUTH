enum AuthStatus {
  NOT_DETERMINED,
  LOGED_IN,
  LOGING_IN,
  FAILED,
}

enum UIState {
  NOT_DETERMINED,
  Waiting,
  QuietWaiting,
  Normal,
}

enum DBState {
  NOT_DETERMINED,
  Working,
  Done,
  Failed,
}
